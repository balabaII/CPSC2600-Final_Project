<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pageTitle %></title>
    <%- include('../includes/links.ejs')%>
  </head>
  <body>
    <div class="container">
      <%- include('../includes/navigation.ejs') %>

      <main>
        <h1 class="text-center">Products</h1>
        <div class="row justify-content-center">
          <% if( products.length > 0) { %> <% for( let product of products ){ %>
          <div class="card col-xs-12 col-sm-6 col-md-4 mx-3">
            <img
              src="/<%= product.image %>"
              alt="product image"
              class="card-img-top"
            />
            <div class="card-body">
              <h2 class="card-title text-center"><%= product.title %></h2>
              <div class="card-text text-center">
                <p><%= product.description %></p>
                <h4><%= product.price %>$</h4>
              </div>
            </div>

            <a
              href="/products/<%= product._id %>"
              class="btn btn-outline-primary"
              >Details</a
            >

            <%- include('../includes/add-to-cart.ejs', {product}) %>
          </div>
          <% } %>
        </div>
        <!-- Just to separate the lines -->
        <% } else{ %>
        <h3 class="text-center">There Are No Products Yet</h3>
        <% } %>
      </main>
      <%- include('../includes/footer.ejs') %>
    </div>
    <%- include('../includes/scripts.ejs') %>
  </body>
</html>
